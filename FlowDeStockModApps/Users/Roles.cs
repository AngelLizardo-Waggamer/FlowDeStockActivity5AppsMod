/**
 * Roles.cs
 * These are the available roles for the users. There are only two roles: Admin and User, but having an enum makes the management easier.
 */

namespace FlowDeStockModApps.Users
{
    internal enum Roles
    {
        ADMIN,
        USER
    }
}
